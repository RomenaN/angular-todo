export interface TaskDataObj {
  taskTitle: string;
  taskType: string;
}

export interface TaskTypes {
  value: string;
  status: string;
  label: string;
}

export interface CountetTask {
  title: string;
  count: number;
  status: string;
}
